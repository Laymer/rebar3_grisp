%--- Erlang Inet Configuration -------------------------------------------------

% Add hosts

%--- Gateway laptop host example --------------------------------------------------

%%{host, {192,168,1,1}, ["mylaptop"]}.

% Do not monitor the hosts file
{hosts_file, ""}.

% Disable caching
{cache_size, 0}.

% Specify lookup method
{lookup, [file, native]}.
